{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src",
    "tsBuildInfoFile": "./dist/.tsbuildinfo",
    // Emit CommonJS: the Electron main process (module: commonjs) require()s this
    // package at runtime, and require() of an ESM build throws ERR_REQUIRE_ESM.
    // Vite (bundler resolution) consumes the CJS build without issue.
    "module": "commonjs",
    "moduleResolution": "node"
  },
  "include": ["src/**/*"],
  "exclude": ["dist", "node_modules"]
}
