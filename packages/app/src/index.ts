/**
 * Placeholder entry point. Phase 3 replaces this with `src/main.tsx` mounting
 * the React tree (ThemeProvider + ShellProviders + AppFrame + TitleBar).
 */
export const APP_VERSION = '0.1.0' as const;
