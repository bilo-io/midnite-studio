import { GraphThemePicker } from '../graph-theme-picker';

/** How the graph is drawn — the live style picker, unchanged from Phase 14. */
export function GraphPage() {
  return <GraphThemePicker />;
}
