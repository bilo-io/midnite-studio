$schema: 'https://moonrepo.dev/schemas/project.json'

language: 'typescript'

dependsOn:
  - 'shared'

tasks:
  # tsc --noEmit resolves @midnite-git/shared through a project reference but does
  # not build it, so build upstream deps first.
  typecheck:
    deps:
      - '^:build'
